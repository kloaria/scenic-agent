# 陈家祠知识库 FAISS 向量数据库

> 基于 FAISS 构建的博物馆知识库向量数据库，适用于 RAG（检索增强生成）应用。

---

## 简介

本向量数据库收录了**广东民间工艺博物馆（陈家祠）**的核心知识库内容，共包含 **2715 个文本向量**，覆盖建筑艺术、馆藏展品、游览路线、参观公告、解说词、方位地图等 9 大主题。

### 核心参数

| 参数 | 说明 |
|:---|:---|
| 向量引擎 | FAISS (IndexFlatIP) |
| 向量维度 | 1024 维 |
| 向量数量 | 2715 个 |
| 相似度度量 | 余弦相似度 |
| Embedding 模型 | BAAI/bge-large-zh-v1.5 |
| 最大分块长度 | 500 字符 |

### 数据来源

本知识库内容来源于陈家祠官方权威资料，确保信息的准确性与权威性：

- [广东民间工艺博物馆官网](http://www.gdmfa.com/)
- 《广州陈氏书院实录》（李卓祺、郑力鹏主编）
- 《陈氏书院》
- 《陈氏书院建筑装饰中的故事和传说》系列丛书

> 开发者使用本知识库时，建议在使用场景中注明数据来源于广东民间工艺博物馆（陈家祠）官方资料。

---

## 文件说明

```
陈家祠向量数据库_交付包/
├── faiss_index.index          # FAISS 索引文件（核心）
├── metadata.json              # 元数据文件（核心）
├── README.md                  # 本文档
├── requirements.txt           # Python 依赖清单
├── example_usage.py           # 基础检索示例
└── rag_integration_example.py # RAG 大模型集成示例
```

### 核心文件

| 文件 | 说明 |
|:---|:---|
| `faiss_index.index` | FAISS 索引文件，包含所有文本块的 1024 维向量。索引类型为 IndexFlatIP，通过向量归一化实现余弦相似度检索。 |
| `metadata.json` | 元数据文件，包含每个向量对应的原文内容、来源文件名和章节标题。字段：`id`、`content`、`source_file`、`chapter`。 |

### 数据分布

| 来源文件 | 文本块数 | 主要内容 |
|:---|:---:|:---|
| 01_整体概况.md | 100 | 陈家祠历史沿革、基本身份 |
| 02_建筑艺术.md | 630 | 木雕、砖雕、石雕、陶塑、灰塑、铜铁铸、彩绘 |
| 03_展厅与展品_长文本.md | 444 | 聚贤堂、西厅、东厅等展厅及展品信息 |
| 04_游览路线.md | 100 | 推荐游览路线 |
| 05_参观公告.md | 91 | 开放时间、票价、预约须知 |
| 06_馆藏藏品_长文本.md | 861 | 陶瓷、刺绣、剪纸、雕刻等馆藏精品 |
| 07_内部方位地图.md | 115 | 洗手间、展厅、服务设施方位 |
| 08_解说词.md | 117 | 导游讲解词 |
| 10_保护与维修.md | 257 | 文物保护与修缮记录 |

---

## 环境要求

- **Python**: 3.8 或更高版本
- **操作系统**: Windows / Linux / macOS
- **内存**: 建议 4GB 以上（模型加载后约占用 1-2GB）
- **磁盘空间**: 模型缓存约需 1.3GB

---

## 环境配置（从零开始）

### 1. 确认 Python 版本

本仓库需要 Python 3.8 或更高版本。在终端中执行：

```bash
python --version
```

确认输出为 `Python 3.8.x` 或更高。若版本过低，请前往 [python.org](https://www.python.org/downloads/) 下载安装。

### 2. 克隆/下载本项目

将交付包解压到你的工作目录，例如：

```
D:\chenjiaci_vector_db
```

### 3. 创建虚拟环境（强烈推荐）

在项目根目录下打开终端，执行：

**Windows：**

```bash
python -m venv venv
```

**macOS / Linux：**

```bash
python3 -m venv venv
```

### 4. 激活虚拟环境

**Windows（PowerShell）：**

```bash
venv\Scripts\activate
```

> ⚠️ 如果报错"无法加载文件 Activate.ps1，因为在此系统上禁止运行脚本"，请先执行：
>
> ```powershell
> Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
> ```
>
> 输入 `Y` 确认，然后重新运行 `venv\Scripts\activate`。

**Windows（CMD）：**

```bash
venv\Scripts\activate.bat
```

**macOS / Linux：**

```bash
source venv/bin/activate
```

激活成功后，终端提示符前会出现 `(venv)` 字样。

### 5. 验证虚拟环境是否激活

执行以下命令，确认当前使用的是虚拟环境中的 Python：

**Windows：**

```bash
where python
```

**macOS / Linux：**

```bash
which python
```

输出路径应包含 `venv` 文件夹（例如 `.../venv/Scripts/python.exe`）。如果路径指向系统目录，说明激活失败，请重新激活。

### 6. 升级 pip（推荐）

```bash
python -m pip install --upgrade pip
```

### 7. 安装依赖包

确保虚拟环境已激活（终端有 `(venv)` 前缀），执行：

```bash
pip install -r requirements.txt
```

如果下载速度慢，可使用国内镜像源：

```bash
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

如果安装过程中报错，请参考下方"常见问题"章节。

### 8. 配置模型下载镜像（可选，国内用户推荐）

为避免下载 Embedding 模型时网络超时，在终端中设置环境变量：

**Windows PowerShell：**

```powershell
$env:HF_ENDPOINT = "https://hf-mirror.com"
```

**macOS / Linux：**

```bash
export HF_ENDPOINT="https://hf-mirror.com"
```

或在 Python 代码开头永久设置：

```python
import os
os.environ["HF_ENDPOINT"] = "https://hf-mirror.com"
```

### 9. 验证环境

运行示例脚本：

```bash
python example_usage.py
```

如果输出检索结果，则环境配置成功。

---

## 快速开始

### 1. 加载向量数据库并执行检索

```python
import os
import json
import shutil
import tempfile
import numpy as np
import faiss
from sentence_transformers import SentenceTransformer

# 配置路径（请根据实际存放位置修改）
DB_DIR = r"./陈家祠向量数据库_交付包"  # 向量数据库所在目录
INDEX_PATH = os.path.join(DB_DIR, "faiss_index.index")
METADATA_PATH = os.path.join(DB_DIR, "metadata.json")

# 加载索引
index = faiss.read_index(INDEX_PATH)
print(f"索引维度: {index.d}, 向量数: {index.ntotal}")

# 加载元数据
with open(METADATA_PATH, "r", encoding="utf-8") as f:
    metadata = json.load(f)

# 加载 Embedding 模型
model = SentenceTransformer("BAAI/bge-large-zh-v1.5")

# 执行检索
query = "陈家祠的建筑特色是什么？"
query_vec = model.encode([query], normalize_embeddings=True).astype("float32")
distances, indices = index.search(query_vec, top_k=5)

# 输出结果
for rank, (idx, score) in enumerate(zip(indices[0], distances[0]), 1):
    meta = metadata[idx]
    print(f"[{rank}] 相似度: {score:.4f}")
    print(f"    来源: {meta['source_file']}")
    print(f"    内容: {meta['content'][:150]}...")
    print()
```

### 完整运行示例

我们也提供了可直接运行的脚本：

```bash
python example_usage.py
```

---

## 集成到大模型（RAG）

典型的 RAG 流程：用户提问 -> 向量检索 -> 构造 Prompt -> 调用大模型 -> 返回回答。

参考示例脚本：

```bash
python rag_integration_example.py
```

### 提示词模板示例

```
你是一位陈家祠（广东民间工艺博物馆）的专业导游。请根据以下参考资料回答用户的问题。

参考资料：
{retrieved_context}

用户问题：{user_question}

请用中文回答。如果参考资料不足以回答问题，请诚实说明。
```

### 支持的模型

本示例默认使用 OpenAI API 格式，但可轻松替换为：

| 服务商 | 替换方式 |
|:---|:---|
| **DeepSeek** | 修改 `base_url` 为 `https://api.deepseek.com/v1` |
| **智谱 GLM** | 修改 `base_url` 为 `https://open.bigmodel.cn/api/paas/v4/` |
| **Azure OpenAI** | 修改 `base_url` 和 `api_key` 为 Azure 配置 |
| **本地模型** | 替换为 Ollama / vLLM / Xinference 等本地推理框架 |

---

## 常见问题

### Q1: `pip install -r requirements.txt` 时报错 `MemoryError`

**原因**：通常是由于 pip 缓存损坏、磁盘空间不足或 pip 版本过低引起。

**解决方案**：

1. **清理缓存**：`pip cache purge` 后重新安装
2. **升级 pip**：`python -m pip install --upgrade pip` 后重试
3. **不使用缓存**：`pip install --no-cache-dir -r requirements.txt` 强制重新下载
4. **分步安装**：先安装核心包 `pip install numpy faiss-cpu sentence-transformers`，再安装其他依赖
5. **检查磁盘空间**：确保系统盘（通常为 C 盘）剩余空间大于 5GB

### Q2: 安装依赖后仍然报 `ModuleNotFoundError: No module named 'numpy'`

**典型现象**：已执行 `pip install -r requirements.txt` 且显示安装成功，但运行脚本时却报错 `No module named 'numpy'`。

**根本原因**：`pip install` 将包安装到了**系统全局 Python 环境**，而不是虚拟环境。通常是因为虚拟环境未激活，或终端使用的 Python 与安装时不一致。

**解决方案**：

1. 确认终端提示符前有 `(venv)` 字样。如果没有，请先激活虚拟环境
2. 执行 `where python`（Windows）或 `which python`（Mac/Linux），确认路径指向 `.../venv/Scripts/python.exe`
3. 在虚拟环境激活状态下重新执行 `pip install -r requirements.txt`
4. 如果使用 VSCode，按 `Ctrl+Shift+P` → `Python: Select Interpreter`，选择 `venv` 环境

### Q3: Windows PowerShell 中激活虚拟环境报错"无法加载文件 Activate.ps1，因为在此系统上禁止运行脚本"

**原因**：Windows 默认禁止执行 `.ps1` 脚本。

**解决方案**：

1. **推荐**：执行 `Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser`，输入 `Y` 确认，然后重新运行 `venv\Scripts\activate`
2. **备选**：将 VSCode 终端切换为 `Command Prompt`，然后使用 `venv\Scripts\activate.bat` 激活
3. **管理员方式**：以管理员身份运行 PowerShell，执行 `Set-ExecutionPolicy RemoteSigned`

### Q4: FAISS 加载索引时报"No such file or directory"，但文件确实存在

**原因**：FAISS 的 C++ 底层不支持路径中的空格（如 `trae data`）或中文字符。

**解决方案**：

**方案 A**（推荐）：将 `faiss_index.index` 和 `metadata.json` 移至纯英文路径（如 `D:\vector_db\`），并修改代码中的路径：

```python
DB_DIR = r"D:\vector_db"  # 纯英文路径
```

**方案 B**：使用临时文件中转：

```python
import shutil, tempfile

def load_index_safe(index_path):
    tmp_dir = tempfile.gettempdir()
    tmp_path = os.path.join(tmp_dir, "faiss_tmp.index")
    shutil.copy(index_path, tmp_path)
    index = faiss.read_index(tmp_path)
    os.remove(tmp_path)
    return index
```

### Q5: 下载 Embedding 模型时出现 `WinError 10060` 连接超时

**原因**：国内网络无法直接访问 HuggingFace 官网。

**解决方案**：在代码开头或终端中设置镜像源环境变量：

**Windows PowerShell：**

```powershell
$env:HF_ENDPOINT = "https://hf-mirror.com"
```

**macOS / Linux：**

```bash
export HF_ENDPOINT="https://hf-mirror.com"
```

**Python 代码中：**

```python
import os
os.environ["HF_ENDPOINT"] = "https://hf-mirror.com"
```

### Q6: 执行 `model.encode()` 时程序卡住，没有输出

**原因**：sentence-transformers 在多线程环境下可能因 PyTorch 死锁而卡住。

**解决方案**：在代码开头添加以下优化设置：

```python
import os
os.environ["OMP_NUM_THREADS"] = "1"
os.environ["MKL_NUM_THREADS"] = "1"

import torch
torch.set_num_threads(1)
torch.set_default_device("cpu")
```

并指定模型加载到 CPU：

```python
model = SentenceTransformer("BAAI/bge-large-zh-v1.5", device="cpu")
```

### Q7: 读取 `metadata.json` 时报 `UnicodeDecodeError`

**原因**：Windows 下默认编码为 `gbk`，但文件为 `utf-8` 编码。

**解决方案**：在 `open()` 中显式指定编码：

```python
with open("metadata.json", "r", encoding="utf-8") as f:
    metadata = json.load(f)
```

### Q8: symlink 警告（huggingface_hub 缓存系统不支持软链接）

**原因**：Windows 需要管理员权限或开发者模式才能创建软链接。

**解决方案**：该警告不影响使用，可忽略。或设置环境变量禁用警告：

```python
import os
os.environ["HF_HUB_DISABLE_SYMLINKS_WARNING"] = "1"
```

### Q9: 检索结果不相关怎么办？

1. **调整 top_k**：增加返回结果数量，让更多候选参与筛选
2. **重排序（Rerank）**：使用 bge-reranker 等重排序模型对 top_k 结果二次排序
3. **调整分块策略**：如果需重建索引，可尝试更小的分块长度（如 300 字符）或增加重叠
4. **混合检索**：结合关键词检索（如 BM25）与向量检索，取两者并集

### Q10: 如何更新知识库？

如需新增或修改知识内容，需要重新构建向量数据库：

1. 准备新的 Markdown 文件
2. 修改 `build_faiss.py` 中的 `INPUT_DIR`
3. 运行 `python build_faiss.py`

---

## 许可与来源声明

- **数据版权**：知识内容来源于广东民间工艺博物馆（陈家祠）官方公开资料
- **使用建议**：在商业或公开项目中使用时，建议注明"数据来源于广东民间工艺博物馆（陈家祠）"
- **模型许可**：BAAI/bge-large-zh-v1.5 遵循 [MIT 许可证](https://huggingface.co/BAAI/bge-large-zh-v1.5)

---

## 联系方式

如有问题或建议，请联系项目维护者。

---

*构建日期: 2026-07-26 | 向量数: 2715 | 维度: 1024*
